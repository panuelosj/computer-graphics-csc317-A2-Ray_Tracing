"""Non-graded infrastructure for A2 (Ray Tracing).

This package holds the scene representation (``Ray``, ``Camera``, ``Material``,
``Object`` and the light/geometry dataclasses), a small JSON scene loader, a
binary/ASCII STL reader and the rendering loops. Students do **not** edit these
files; they implement the pure functions in ``src/`` which these helpers drive.
"""
